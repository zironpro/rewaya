/**
 * @deprecated Import from `@/lib/catalog/types` instead.
 * Kept for backward-compatible type imports only — no static catalog data.
 */
export type {
	Book,
	Bundle,
	Faq,
	RelatedBundle,
} from "@/lib/catalog/types";
